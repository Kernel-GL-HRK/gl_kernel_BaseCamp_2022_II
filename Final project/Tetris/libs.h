#ifndef __LIBS_H__
#define __LIBS_H__

#include <ctype.h>	  // toupper()
#include <curses.h>	  //initscr(),start_color(),init_pair(),endwin()
#include <ncurses.h>  //cbreak(), noecho(), macros printw(), macros scanw(), refresh(), clear()
#include <stdio.h>	  // printf(), scanf(), fputs()
#include <stdlib.h>	  //srand(), rand(), malloc(), free(), strtol()
#include <string.h>	  // memcpy(), strncmp()
#include <sys/time.h> // struct timeval, gettimeofday()
#include <time.h>	  // time()
#include <unistd.h>	  // usleep()

#endif